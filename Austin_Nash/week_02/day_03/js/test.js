
const arr = [
  {name: "josh", eyes: "brown"},
  {title: "TA", cost: 50}
]

let quiz = function (arr){
for (var i = 0; i < arr.length; i++) {
  console.log(arr[i].value))
  console.log(arr[i].key))
}
}


const quiz = function (max, factor){
  for (var i = 0; i < quiz.length; i++) {
    if (max % factor === 0) {

      console.log(quiz.pop());
    }else
    console.log(false);
  }
}
quiz(10,5);
