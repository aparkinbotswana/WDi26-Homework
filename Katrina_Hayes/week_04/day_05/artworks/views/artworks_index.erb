<h2>Artworks Index</h2>

<div class="gallery">
  <% @artworks.each do |artwork| %>
    <a href="/artworks/<%= artwork.id %>">
      <img src="<%= artwork.image %>" alt="<%= artwork.name %>">
    </a>
  <% end %>
</div>
